# Keep Docker build context small and stable.
# The relay Dockerfile only needs `crates/splash-relay/**` and Cargo.lock.

# Common large/unneeded dirs
node_modules/
.git/
**/.DS_Store
playwright-report/
test-results/

