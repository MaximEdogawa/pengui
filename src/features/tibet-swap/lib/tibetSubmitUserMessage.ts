/**
 * Single copy for Tibet submit failures in the UI. Raw API messages may
 * contain tracebacks or JSON; those are logged only, never shown.
 */
export const TIBET_SUBMIT_USER_MESSAGE =
  "Could not complete this transaction. Try again or adjust amounts.";
